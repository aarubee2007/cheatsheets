## Turn off warnings

Turn them off for specific function:

```python
import warnings

def fxn():
    warnings.warn("deprecated", DeprecationWarning)

with warnings.catch_warnings():
    warnings.simplefilter("ignore")
    fxn()
```

Turn them off for everythin (not condoned):

```python
import warnings
warnings.filterwarnings("ignore")
```
